# Gotchas — Recall.ai and ElevenLabs

Single-source catalog of every non-obvious thing Recall and ElevenLabs (Scribe transcription provider + Voice TTS) have bitten us on, or are documented to bite us on. Each row cites the file/line where the gotcha is encoded today (or would be, after the documented swap); the repo-root CLAUDE.md cautionary tales are the canonical witness for #1 (2026-05-12) and the spec-fixture rule (2026-05-10 cousin).

| # | Gotcha | Where it's encoded today | Why it matters |
|---|---|---|---|
| 1 | `in_call_recording.data` is a **nested** `{ kind, b64_data }` object at bot-create — NOT a scalar `data: "<b64>"`. | `bot-client.ts:230` (build), `bot-client.ts:38-72` `SILENT_MP3_B64` JSDoc | Production 502 on 2026-05-12. The first probe only checked presence; a fully-populated payload surfaced the type mismatch. |
| 2 | The same payload is **flat** `{ kind, b64_data }` at `/output_audio/` mid-call — no `data:` wrapper. | `bot-client.ts:275` | Asymmetric with create-time. Change one site → audit all three (gotcha #1, #2, any future audio site). |
| 3 | The realtime config key is `realtime_endpoints` (one word). `real_time_endpoints` (with underscore) is **silently ignored**. | `bot-client.ts:202` | Bot dispatch succeeds with 200; no transcripts ever arrive. The original placeholder name in early code was the wrong form. |
| 4 | Webhook payloads (status AND realtime) are **double-nested**: `data.data.<fields>` for the per-event body, `data.bot.<…>` for the bot envelope. | `lib/meet/webhook-handler.ts:80-89` (`StatusEventBody`), 103-113 (`RealtimeTranscriptBody`) | A handler that destructures `body.data` as the payload directly will silently see `undefined` for every field. |
| 5 | `transcript.data` `start_timestamp.relative` is in **seconds** (float), not ms or μs. Seq derivation multiplies by 1e6. | `lib/meet/webhook-handler.ts:467` | Two speakers starting at the exact same μs is the only realistic collision and would drop the second utterance. Acceptable for V1. |
| 6 | Audio cap is **1.8 MB base64** (≈1.35 MB raw mp3). `SILENT_MP3_B64` is 768 chars on purpose — comfortably between Recall's lower "non-trivial" gate and the ceiling. | `bot-client.ts:44-56` `SILENT_MP3_B64` JSDoc | Files over the cap → 4xx from `/output_audio/`. |
| 7 | Chat-message cap is **500 chars on Meet** (4096 on Zoom/Teams). We cap at 500 universally for portability. | `packages/chat-agent-secretary/src/tools.ts::sendChatInMeeting`; bot-client.ts:131 doc | Over-cap → 4xx from `/send_chat_message/`. |
| 8 | Speak cap is **240 chars** — our own enforcement to keep TTS latency low, not Recall's. | `app/api/meet/speak/route.ts` / `speak-handler.ts` | Different from gotcha #7; same molecule but a different limit. |
| 9 | `automatic_leave.silence_detection.activate_after >= 1` (seconds). `0` or negative → Recall 4xx. We use 60 s as a grace window. | `bot-client.ts:218` | Without the floor, silence-detection mis-fires before the bot has joined. |
| 10 | `POST /leave_call/` returning **404 means "bot already gone"** — treat as success, not failure. | `bot-client.ts:259` | Race with manual removal or silence-timeout firing first. Surfacing as error would spam alerts. |
| 11 | Bots **must** be created with `automatic_audio_output` set, or mid-call `/output_audio/` 4xx's. That's why we ship `SILENT_MP3_B64` at create time even if speak is never used. | `bot-client.ts:222-231` + `SILENT_MP3_B64` JSDoc | Documented as required by Recall; verified by probe 2026-05-12. |
| 12 | Recall retries a failed realtime endpoint **60 times at 1 s intervals** then marks it dead. If the route is down for >1 minute, transcripts for that bot stop forever — even after the route recovers. | `app/api/meet/webhook/realtime/route.ts:19-20` doc | Real-time recovery requires re-dispatching the bot, not just bringing the route back. |
| 13 | `RECALL_API_BASE_URL` is **regional**. Wrong region against the workspace key → 401 ("invalid API key"). | [auth-and-env.md](./auth-and-env.md) | The key is workspace-scoped; the region is part of the workspace identity. |
| 14 | ElevenLabs takes **different `model_id` values for real-time vs async**. `elevenlabs_streaming.model_id = "scribe_v2_realtime"` is NOT interchangeable with `elevenlabs_async.model_id = "scribe_v2"`. | [api-reference.md](./api-reference.md) Alternate-providers subsection | Using the wrong one against the wrong endpoint silently produces no transcripts (no error at bot-create, no events on the webhook). |
| 15 | Third-party transcription API keys are stored **per Recall region** in the dashboard. A key registered in `us-east-1` is invisible to a `us-west-2` workspace. | [auth-and-env.md](./auth-and-env.md) "Third-party transcription-provider keys" | Bot-create returns 200; the transcription path silently fails on `bot.in_call_recording`. Symptom looks identical to gotcha #3. |
| 16 | ElevenLabs API keys are **scope-gated**. Voice (TTS) needs Text-to-Speech access; Scribe needs Speech-to-Text access. A "default" key may have only one. | [auth-and-env.md](./auth-and-env.md) "ElevenLabs API key — scope gating" | Voice without TTS scope → `401` from `api.elevenlabs.io`. Scribe without Speech-to-Text scope → bot dispatches OK, no transcripts ever arrive (identical symptom to gotcha #15 region-mismatch and #3 typo). Check key scope in the ElevenLabs dashboard. |
| 17 | ElevenLabs Voice TTS returns **binary audio** (`Content-Type: application/octet-stream`), not JSON. OpenAI TTS's `/v1/audio/speech` also returns binary, so the read pattern in `tts.ts` (`await res.arrayBuffer()` → `Buffer.from(...).toString("base64")`) carries over unchanged — but only because both vendors happen to use the same encoding. Don't assume `await res.json()` based on sibling REST endpoints (`/v1/text-to-speech-stream` uses chunked transfer, `/v1/text-to-speech/{voice_id}/with-timestamps` returns JSON-wrapped). | [elevenlabs.md](./elevenlabs.md) "Response" | A `await res.json()` call against the convert endpoint will throw on the first byte. |
| 18 | ElevenLabs `output_format` query param has **tier-gated values**. `mp3_44100_192` requires Creator tier; all 44.1 kHz PCM/WAV require Pro tier. Free tier picks tier-gated formats → 4xx. | [elevenlabs.md](./elevenlabs.md) "Query params" | Default `mp3_44100_128` is safe on all tiers and decodes cleanly for Recall's `/output_audio/` 1.35 MB raw cap. Don't bump bitrate without confirming the workspace tier. |
| 19 | ElevenLabs uses header **`xi-api-key: <key>`**, NOT `Authorization: Bearer <key>` (Recall and OpenAI both use Authorization). Easy to copy-paste wrong from the OpenAI TTS path. | [elevenlabs.md](./elevenlabs.md) "Headers" | Wrong header → 401 from `api.elevenlabs.io`. The ElevenLabs error body is JSON `{ detail: "..." }`, not the plain text shape Recall returns. |

## Cross-references — cautionary tales

The repo-root `CLAUDE.md` carries two tales that anchor the rules this skill enforces:

- **2026-05-12** — `in_call_recording.data` nesting (gotcha #1). Lives in CLAUDE.md → "READ THE VENDOR'S CURRENT DOCUMENTATION FIRST". The fix path was: read live doc → probe sandbox → translate exact shape back. Same root cause appeared at OpenAI Realtime the next day.
- **2026-05-10** — Web Speech `event.resultIndex` regression (cousin to gotcha #5). Lives in CLAUDE.md → "Test fixtures for browser / native APIs must be spec-faithful, not happy-path-shaped". The lesson: a one-event fixture cannot catch the cumulative-shape class of bug. Applies directly to `transcript.data` testing — see [webhooks.md](./webhooks.md) "Spec-faithful fixture — worked example".

When fixing a Recall bug, add a new row to this file in the same PR if the bug class was novel — it's the single place future agents come to for "what should I know that the docs don't say."
